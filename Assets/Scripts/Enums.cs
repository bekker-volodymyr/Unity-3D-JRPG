using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enums 
{
    public enum State
    {
        Idle, Fight
    }

    public enum PlayerState
    {
        Idle, Fight, Attack
    }
}
